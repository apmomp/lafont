- if object.errors.any?
  #error_explanation
    %h2
      = pluralize(object.errors.count, "error evita", "errores evitan")
      que este 
      = object.class.to_s.underscore.humanize.downcase
      pueda ser guardado:
    %p
      Hay problemas con los siguientes campos:
    %ul
      - object.errors.full_messages.each do |msg|
        %li
          = msg
        
